import React from "react"
import Layout from "../components/layout"

export default function SeriesSingle({data}) {
  console.log('data',data);
  return (
    <Layout>
      <div>Hello blog post</div>
    </Layout>
  )
}